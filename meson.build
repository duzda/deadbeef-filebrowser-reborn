project('filebrowser', 'c', 'cpp',
    # version: 'a.b.0' -> a gets set as major, b as minor, 0 because it's never used
    version: '0.4.0',
    default_options: ['c_std=c17', 'cpp_std=c++17'],
)

subdir('src')
